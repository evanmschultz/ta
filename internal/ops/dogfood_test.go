package ops_test

// The pre-F10 dogfood test exercised the `mage dogfood` target which
// is now retired (PLAN §12.17.9 F10 Q1). Coverage of `ops.Create` /
// `ops.Get` / `ops.Update` / `ops.Delete` lives in ops_test.go.
