<article aria-labelledby="version-title">
  <header>
    <div class="version-tag">roadmap.version</div>
    <h2 id="version-title">v0.1.0</h2>
    <div class="target-band">Q3 2026</div>
    <div class="pills" aria-label="Version metadata">
      <span class="pill" data-key="status" data-value="in_progress">in_progress</span>
    </div>
  </header>

  <section aria-label="Vision summary">
    <h3>Vision</h3>
    <pre class="prose">Pre-MVP-feature-complete ta launches clean. Goal: every MVP feature works without known issues. Attack payload: &lt;script&gt;alert(&#39;xss&#39;)&lt;/script&gt;</pre>
  </section>

  
  <section aria-label="Rationale">
    <h3>Rationale</h3>
    <pre class="prose">Dogfood phase showed enough rough edges that a structured pre-MVP-feature-completion sweep &#43; zero-tech-debt gate is justified before tagging.</pre>
  </section>
  

  
  <section aria-label="Scope">
    <h3>Scope</h3>
    <pre class="prose">1. close huh removal (F38d series)
2. F23 runtime-fill semantics
3. TUI verification artifacts (gifs &#43; ascii) committed
4. cmd/ta coverage gate ≥70%</pre>
  </section>
  

  
  <section aria-label="Milestones">
    <h3>Milestones</h3>
    <ul class="flat">
      <li>F38d huh removal complete</li><li>F23 token expansion shipped</li><li>TUI verification artifacts under cmd/ta/testdata/vhs/</li><li>cmd/ta coverage ≥70%</li>
    </ul>
  </section>
  

  

  

  

  
  <footer>
    Rendered from ta record <code>roadmap.version</code>. Track A (zero-deps) template.
  </footer>
  
</article>

<style>
   

  article {
    display: block;
  }

  .version-tag {
    font-size: var(--text-sm);
    color: var(--text-secondary);
    font-family: var(--font-mono);
  }

  article h2 {
    margin: var(--space-3) 0 0;
    font-size: var(--text-2xl);
    line-height: var(--leading-snug);
  }

  .target-band {
    font-size: var(--text-sm);
    color: var(--text-secondary);
    margin-top: var(--space-1);
  }

  .pills {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2);
    margin-top: var(--space-2);
  }

  .pill {
    display: inline-block;
    padding: var(--space-1) var(--space-3);
    border-radius: var(--radius-pill);
    font-size: var(--text-xs);
    background: var(--bg-selected);
    color: var(--accent);
    font-weight: var(--weight-medium);
  }

  .pill[data-key="status"][data-value="planning"] {
    background: #e5e7eb;
    color: #4b5563;
  }

  .pill[data-key="status"][data-value="in_progress"] {
    background: #dbeafe;
    color: #1f4ed8;
  }

  .pill[data-key="status"][data-value="landed"] {
    background: #d1fae5;
    color: #047857;
  }

  .pill[data-key="status"][data-value="punted"] {
    background: #e5e7eb;
    color: #6b7280;
  }

  article section {
    margin-block: var(--space-6);
  }

  article section h3 {
    font-size: var(--text-lg);
    margin: 0 0 var(--space-2);
    color: var(--text-primary);
  }

  dl.meta {
    display: grid;
    grid-template-columns: max-content 1fr;
    column-gap: var(--space-4);
    row-gap: var(--space-1);
    margin: 0;
  }

  dl.meta dt {
    color: var(--text-secondary);
    font-size: var(--text-sm);
  }

  dl.meta dd {
    margin: 0;
    font-size: var(--text-base);
    word-break: break-word;
  }

  ul.flat {
    margin: 0;
    padding-left: var(--space-5);
  }

  ul.flat li {
    margin-block: var(--space-1);
  }

  pre.prose {
    white-space: pre-wrap;
    font-size: var(--text-base);
    color: var(--text-primary);
    margin: 0;
    font-family: var(--font-sans);
    background: transparent;
    border: 0;
    padding: 0;
  }

  article footer {
    margin-top: var(--space-8);
    padding-top: var(--space-4);
    border-top: 1px solid var(--bg-hover);
    font-size: var(--text-xs);
    color: var(--text-secondary);
  }

  @media (max-width: 767px) {
    article h2 {
      font-size: var(--text-xl);
    }

    dl.meta {
      grid-template-columns: 1fr;
    }

    dl.meta dt {
      margin-top: var(--space-2);
    }
  }
</style>
